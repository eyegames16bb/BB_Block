* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
  user-select: none;
}

body {
  background-color: #1a0f07;
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 100vh;
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}

/* Ana Oyun Çerçevesi */
.game-container {
  width: 360px;
  background: #8c532b;
  background: linear-gradient(180deg, #9e5d32 0%, #6d3a19 100%);
  border-radius: 20px;
  padding: 16px;
  box-shadow: 0 10px 25px rgba(0, 0, 0, 0.5), inset 0 2px 4px rgba(255, 255, 255, 0.2);
  display: flex;
  flex-direction: column;
  gap: 12px;
  border: 4px solid #4a250e;
}

/* Üst Bar */
.header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 8px;
}

.icon-btn {
  width: 40px;
  height: 40px;
  background: linear-gradient(180deg, #c4824d 0%, #7d441e 100%);
  border: 2px solid #4a250e;
  border-radius: 10px;
  display: flex;
  justify-content: center;
  align-items: center;
  font-size: 20px;
  box-shadow: 0 4px 0 #321808;
  cursor: pointer;
}

.score-container {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.score {
  background: #4a250e;
  color: #f7d297;
  font-weight: bold;
  font-size: 18px;
  padding: 4px 16px;
  border-radius: 12px;
  border: 2px solid #6d3a19;
  box-shadow: inset 0 2px 4px rgba(0, 0, 0, 0.5);
}

.progress-bar {
  width: 100%;
  height: 10px;
  background: #321808;
  border-radius: 5px;
  margin-top: 4px;
  overflow: hidden;
  border: 1px solid #6d3a19;
}

.progress-fill {
  width: 50%;
  height: 100%;
  background: linear-gradient(90deg, #e69d37, #fce07e);
  border-radius: 5px;
}

.chest-icon {
  font-size: 28px;
  filter: drop-shadow(0 2px 4px rgba(0,0,0,0.4));
}

/* Oyun Tahtası (Izgara) */
.grid-container {
  background: #3a1e0b;
  border: 4px solid #241105;
  border-radius: 12px;
  padding: 6px;
  display: grid;
  grid-template-columns: repeat(8, 1fr);
  gap: 3px;
  aspect-ratio: 1;
  box-shadow: inset 0 4px 8px rgba(0, 0, 0, 0.6);
}

.cell {
  background: #4a2810;
  border-radius: 3px;
  box-shadow: inset 0 2px 4px rgba(0, 0, 0, 0.4);
}

/* Kontrol Butonları */
.controls {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 0 4px;
}

.action-btn {
  width: 42px;
  height: 38px;
  background: linear-gradient(180deg, #d8965d 0%, #8c4e23 100%);
  border: 2px solid #321808;
  border-radius: 8px;
  font-size: 18px;
  box-shadow: 0 3px 0 #241105;
  cursor: pointer;
}

.powerup-btn {
  background: #4a250e;
  border: 2px solid #6d3a19;
  border-radius: 8px;
  display: flex;
  align-items: center;
  padding: 2px 4px 2px 8px;
  gap: 6px;
  color: #f7d297;
  font-weight: bold;
}

.add-btn {
  width: 24px;
  height: 24px;
  background: linear-gradient(180deg, #e6a15c 0%, #a65e28 100%);
  border: 1px solid #321808;
  border-radius: 6px;
  color: #fff;
  font-weight: bold;
  cursor: pointer;
}

/* Parça Yuvaları */
.piece-dock {
  background: #4a250e;
  border: 3px solid #321808;
  border-radius: 12px;
  height: 100px;
  display: flex;
  justify-content: space-around;
  align-items: center;
  box-shadow: inset 0 2px 6px rgba(0, 0, 0, 0.5);
}

.piece-slot {
  width: 80px;
  height: 80px;
  display: flex;
  justify-content: center;
  align-items: center;
}

/* Footer / Shop */
.footer {
  display: flex;
  justify-content: space-between;
  align-items: flex-end;
  padding-top: 4px;
}

.game-title {
  color: #f7d297;
  display: flex;
  flex-direction: column;
  line-height: 1.1;
  text-shadow: 0 2px 4px rgba(0, 0, 0, 0.5);
}

.game-title span { font-size: 14px; }
.game-title strong { font-size: 18px; letter-spacing: 1px; }

.shop-btn {
  background: linear-gradient(180deg, #c4824d 0%, #7d441e 100%);
  border: 2px solid #321808;
  border-radius: 10px;
  padding: 4px 8px;
  box-shadow: 0 3px 0 #241105;
  display: flex;
  flex-direction: column;
  align-items: center;
  cursor: pointer;
}

.coins {
  display: flex;
  align-items: center;
  gap: 2px;
  background: #321808;
  padding: 2px 8px;
  border-radius: 10px;
  color: #fff;
  font-size: 12px;
  font-weight: bold;
}

.shop-label {
  color: #f7d297;
  font-size: 11px;
  font-weight: bold;
  margin-top: 2px;
}