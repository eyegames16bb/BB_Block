# BB Block

10x10 ahşap temalı blok bulmaca oyunu (iOS + Android). Oyuncular rastgele 3 poliomino parçasını 10x10 ızgaraya yerleştirir, tam satır/sütun oluşturarak puan kazanır. İki mod: **Klasik Mod** (serbest oynanış, Çerçeve VAR/YOK seçeneği) ve **Level Mod** (1000 puan hedefi, 900 puanda çerçeve kalkar).

Kaynak tasarım dokümanı: [`Dökümasyon Proje Tanımı.md`](./Dökümasyon%20Proje%20Tanımı.md) — GDD metni "9x9" diyor, ama kullanıcı bu oturumda tahtayı **10x10** olacak şekilde düzeltti (`BoardConstants.gridSize`, tek doğruluk kaynağı); GDD dosyasının kendisine dokunulmadı, yalnızca kod ve bu dosya güncel değeri yansıtıyor. Referans görseller: `Arayüz Örnek Görseller/`, `Blok Parçaları Örnek Görseller/` — bunlar üretim varlığı değil, ilham/mockup amaçlıdır. Klasördeki önceki çoklu-görsel seti (genel stil ilhamı amaçlı) kullanıcı tarafından silinip yerine tek bir referans görsel (`BB Block Örnek Arayüz Referans.jfif`) ve onu üreten `Arayüz Örnek Görselin Html ve Css Kodları/` (html.txt + css.txt) ile değiştirildi — bu sefer istek "birebir"/"komple" bir yeniden üretimdi, önceki "stil ilhamı al ama kendi tasarımını kat" yaklaşımından daha literal (bkz. "Bilinen açık noktalar").

## Teknoloji

Flutter (stable) + Dart, Clean Architecture, Feature-First klasör yapısı.

- **State**: Riverpod (`flutter_riverpod` + `riverpod_annotation`/`riverpod_generator`, codegen tabanlı). `get_it` kullanılmıyor — Riverpod tek başına DI konteyneri.
- **Routing**: `go_router`
- **Codegen**: `freezed` + `json_serializable` + `build_runner`
- **Lint**: `very_good_analysis` (`flutter_lints` değil — ikisi birlikte kullanılmaz). `public_member_api_docs` kuralı kapalı: yorum yazma kuralımız zaten "yalnızca non-obvious olan yerde" prensibine dayanıyor.
- **Local storage**: `shared_preferences` üzerinden tek JSON blob (`LocalGameSaveRepository`). Bilinçli olarak Isar/Hive değil — oyunun kalıcı veri ihtiyacı küçük (birkaç sayaç). `GameSaveRepository` arayüzü sayesinde ileride Isar/Hive/Drift'e geçiş yalnızca `features/persistence/data/` içinde kalır.
- **Cloud backend**: Supabase (karar verildi — açık kaynak/uzun vadeli tercih gerekçesiyle Firebase yerine). `google_mobile_ads` backend'den bağımsız olduğu için reklam tarafı etkilenmiyor.
- **Reklam**: `google_mobile_ads` (AdMob). `AdMobAdsService` şu an Google'ın herkese açık **test** ad unit ID'lerini kullanıyor — mağaza gönderiminden önce gerçek ID'lerle değiştirilmeli.
- **IAP**: `purchases_flutter` (RevenueCat)
- **Ses**: `audioplayers` (çakışan çoklu SFX + döngülü ambiyans). Oynanış SFX'lerinin çoğu artık gerçek asset yerine `procedural_sfx.dart`'ta çalışma zamanında sentezlenen 16-bit PCM WAV'lar — bkz. "Bilinen açık noktalar".
- **Haptik**: `vibration`
- **Partikül**: `newton_particles` — `BoardGrid`'de gerçek toz patlaması efekti için kullanılıyor, bkz. "Bilinen açık noktalar".
- **İkon**: `phosphoricons_flutter` (**`phosphor_flutter` değil** — o paket `IconData`'yı extend ediyor, ve şu anki Flutter SDK (3.44.7) `IconData`'yı `final class` yaptığı için artık derlenmiyor; bkz. [breaking change](https://docs.flutter.dev/release/breaking-changes/icondata-class-marked-final). `phosphoricons_flutter` aynı ikon setini typedef tabanlı sabitlerle sağlıyor: `PhosphorIcons.gear` gibi çağrısız `static const IconData`, `PhosphorIcons.crownFill`/`keyFill` gibi stil-sonekli varyantlar)
- **Font**: Google Fonts — Fredoka (başlık/display) + Nunito (gövde), `google_fonts` paketiyle çalışma zamanında yükleniyor
- **Uygulama ikonu/görseller**: `assets/icon/icon.png` + `assets/images/home_background.png` (pubspec `assets:` altında, kaynak orijinalleri repo kökündeki `images/`'ten kopyalandı) — `flutter_launcher_icons` (dev_dependency, `pubspec.yaml`'da `flutter_launcher_icons:` bloğu) bu ikondan Android (`mipmap-*`) ve iOS (`AppIcon.appiconset`) tüm boyutları üretiyor, bkz. "Bilinen açık noktalar".

## Mimari

```
lib/
  core/
    constants/     — büyü sayı yok: board boyutu, puan sabitleri, level eşiği burada
    theme/         — renk/tipografi token'ları + `WoodBackground` (procedural wood-grain `CustomPainter`)
    routing/       — go_router
    providers/     — Audio/Haptics/Ads/Persistence/Game Feel servisleri için Riverpod seam'leri
    services/      — Audio/Haptics/Ads: arayüz + implementasyon ayrı dosyalarda (SRP)
    game_feel/     — Game Feel Engine: SpringPressable, ScreenShakeController/ScreenShake, FeedbackOrchestrator (bkz. "Bilinen açık noktalar")
    utils/
  features/
    board/domain/           — Board, Cell, PieceShape, GridPosition; PlacementValidator, LineClearResolver
    piece_generation/domain — Factory Pattern: WeightedPieceGenerator (ağırlıklı rastgele + çözülebilirlik garantisi)
    scoring/domain           — Strategy Pattern: ClassicScoringStrategy / LevelScoringStrategy
    game_mode/domain         — Strategy Pattern: GameModeStrategy + RoundOutcome
    game_engine/domain       — Orkestrasyon: GameEngine + GameSession + GameEvent (saf Dart)
    booster/domain           — Command Pattern: Rotate/Swap/SingleCellRemove
    persistence/domain+data   — Repository Pattern: GameSaveRepository + local (SharedPreferences) impl
    persistence/application    — PlayerProgressController (yüksek skor/level/altın anahtar, ses/haptik tercihi, kalıcı)
    settings/presentation      — SettingsSheet (Ses/Titreşim toggle'ları; her iki ekranın gear butonundan açılıyor)
    game/application          — GameController (Riverpod Notifier) + GameLaunchConfig + game_feel_mapping.dart (katmanlı ses/shake eşlemesi)
    game/presentation         — GameScreen + BoardGrid/PieceTray/BoosterBar/PieceView (oynanabilir döngü, `GamePalette` renk token'ları)
    home/presentation         — ana menü (mod seçimi, Çerçeve/Level Mod başlangıç sheet'leri, `PremiumGameButton`)
```

Engine (board/scoring/piece_generation/booster/game_engine) UI'dan tamamen bağımsız — bu dosyaların hiçbiri `flutter/*` import etmez, yalnızca `application`/`presentation` katmanları eder. Bu ayrım korunmalı. UI, `GameController` üzerinden yalnızca `GameSession` state'ini okur ve intent metodları çağırır; kurallar widget katmanına sızmaz.

## Alınan mimari kararlar (kod yazılmadan önce netleşti)

1. **Backend**: Supabase.
2. **Undo**: Yok. Command Pattern yalnızca 3 tamamlayıcı için.
3. **Puanlama**: GDD metni aynen — Çerçeve VAR=8, Çerçeve YOK=9, Level&lt;900=8, Level≥900=9 puan/sıra. Kasıtlı zorluk dengesi olarak kabul edildi.
4. **Çerçeve kaldırma efekti**: GDD'nin "aynı animasyon" ifadesi yerine ayrı bir **Frame Destroy** efekti kullanılıyor — `SoundEffect.frameDestroy` (ses) + `HapticIntensity.heavy` (haptik) + `BoardGrid._frameClearingOverlay` (görsel: sıra temizlemeden daha büyük/yavaş, `Interval` ile sol-üstten dışa doğru gecikmeli dalga) + gerçek `newton_particles` toz patlaması (bkz. "Bilinen açık noktalar").
5. **Level ilerleme**: v1'de tek düzey davranış — her level aynı kurallarla çalışır, level numarası yalnızca ilerleme sayacı. Zorluk eğrisi tasarımı ertelendi (mimari `GameModeStrategy` ile buna hazır).
6. **Tamamlayıcı şarj modeli — round-scoped, kalıcı DEĞİL (bu oturumda ikinci kez tersine çevrildi — açık kullanıcı talimatı, GDD'nin "her deneme taze başlar" metnine dönüş)**: Klasik Mod'da tamamlayıcı hiç yok (GDD: "Bu oyun modunda tamamlayıcı olmayacaktır"). Level Mod'a girişte `HomeScreen` artık ikili bir seçim sunuyor (`_LevelStartSheet`): **"Altın Anahtar İle Oyuna Başla"** (1 Altın Anahtar harcanır, o round için her tamamlayıcıdan tam olarak 1 şarj — `BoosterConstants.unlockedChargesPerRound`) veya **"Anahtarsız Oyuna Başla"** (üç tamamlayıcı da 0 şarjla başlar). Hangisi seçilirse seçilsin şarjlar **o round'a özel** — round bitince (kazanılsa da kaybedilse de) sıfırlanır, sonraki level'a hiçbir şey taşınmaz, ve round SIRASINDA reklam izleyerek ya da başka bir Altın Anahtar harcayarak şarj eklemek **mümkün değildir** (önceki turun "0 şarja düşen booster'a dokunup anahtarla doldur" davranışı tamamen kaldırıldı). Bu, bu oturumun daha önceki bir noktasında kurulan "hesap genelinde kalıcı şarj + mid-round anahtarla doldurma" modelinin (aşağıdaki eski metin buradaydı) doğrudan tersidir — kullanıcı ikisini de aynı oturumda istedi, en son talimat geçerli.
   - **Kod tarafı**: `GameLaunchConfig.levelBoostersUnlocked` (bool) geri geldi — `HomeScreen._startLevel`, sheet'ten dönen seçime göre `PlayerProgressController.spendGoldKeyForBoosters()` çağırıp (yalnızca anahtar seçildiyse) bu bool'u set ediyor. `GameController.build()` artık `PlayerProgress`'i hiç okumuyor — `config.levelBoostersUnlocked` doğruysa üç `initialXCharges`'i `BoosterConstants.unlockedChargesPerRound` (1), değilse 0 yapıp `GameEngine`'i tohumluyor. `PlayerProgress.rotateCharges/swapCharges/singleCellRemoveCharges` alanları tamamen silindi (artık kalıcı bir şey yok, saklayacak bir şey de yok); `PlayerProgressController.syncBoosterCharges`/`refillBooster` kaldırıldı, yerine tek satırlık `spendGoldKeyForBoosters()` geldi. `BoosterBar` artık `goldKeyCount`/`onRefill` almıyor — 0 şarjlı bir buton basitçe pasif, anahtar rozetine dönüşmüyor. Artık kullanılmayan `BoosterKind` enum'u (`features/booster/domain/booster_kind.dart`) silindi.
   - **Yarış durumu notu (hâlâ geçerli)**: `GameController.build()` artık `PlayerProgress`'e hiç bakmadığı için PlayerProgress'in yüklenmesini beklemeye gerek kalmadı gibi görünse de, `GameScreen`/`HomeScreen._startLevel`'daki bekleme hâlâ duruyor — `progress.goldKeyCount`'un header/footer rozetlerinde ve start sheet'inde doğru gösterilmesi hâlâ `PlayerProgressController`'ın yüklenmiş olmasını gerektiriyor.
7. **Combo**: Ekstra puan/çarpan yok. Çoklu sıra temizleme = `lineCount × pointsPerClearedLine`, doğrusal. "Combo"/"Multiple Line Complete" yalnızca ses/animasyon geri bildirimi olarak kalıyor, puanlamayı etkilemiyor.
8. **HUD "+" butonları — kapsam dışı bırakıldı**: Mockup'taki ayrı inline "+" butonları eklenmedi. Karar #6'nın güncel (round-scoped) modelinde mid-round doldurma diye bir şey yok, tek "doldurma" noktası Level Mod'a girmeden ÖNCEki `_LevelStartSheet` — o da ayrı bir "+" widget'ı değil, ikili bir başlangıç seçimi.

## Bilinen açık noktalar (henüz kapatılmadı)

- **`BoosterBar` "çerçeve" görünümünden madalyon/token tasarıma geçti (bu oturumda yapıldı — kullanıcı yeni referans görseli sağladı)**: Önceki tasarım köşeli, düz gradyanlı bir panel + beyaz kenarlıklı rozet çemberiydi ("çerçeve gibi" hissettiriyordu). `_BoosterButton` artık dairesel bir "ahşap madalyon" — `RadialGradient` ile parlak/derin ahşap tonu geçişi, üstte `PhosphorIconsBold` ikonlar (`arrowsClockwise`/`swap`/`eraser`), altta etiket ("Yön"/"Değiştir"/"Sil"), şarj rozeti artık beyaz kenarlıksız, madalyonun kendi alt kenarına gömülü küçük bir daire. `PieceTray`'deki "silahlanmış" parça göstergesi de aynı gerekçeyle düz `Border.all()` yerine yumuşak altın parıltı (`boxShadow` glow) kullanıyor. Ana menüde mod butonlarına da referans görseldeki gibi ikon eklendi (`PhosphorIconsFill.crown` → Klasik Mod, `PhosphorIconsFill.mountains` → Level Mod). `GameScreen`'deki `reservedHeight` (booster satırı için ayrılan dikey alan) yeni daha uzun tasarıma göre 88'den 118'e çıkarıldı. Emülatörde hem dolu hem sıfırlanmış (`pm clear`) hesapla doğrulandı — yeni oyuncu üç tamamlayıcıda da "1" ile başlıyor (karar #6'nın davranışı zaten doğruydu, yalnızca görsel değişti).
- **`PlayerProgressController` mutasyonları artık serileştiriliyor (bu oturumda düzeltildi — gerçek bir yarış hatası)**: Her mutator (`refillBooster`, `grantGoldKey`, `advanceLevel`, `recordClassicScore`, `syncBoosterCharges`, ses/haptik toggle'ları) `state.value`'u okuyup değiştirip geri yazıyordu, aralarında hiçbir eşzamanlılık koruması yoktu. Somut istismar: tükenmiş bir booster butonuna hızlı çift dokunuş — her iki `refillBooster` çağrısı da AYNI (henüz yazılmamış) `goldKeyCount`'u okuyup ikisi de başarılı oluyor, tek anahtarla iki şarj kazandırıyor (hatta ikinci çağrının `copyWith`'i birinci çağrının şarj artışını bilmediği için üzerine yazıp tamamen kaybediyor). `_serialized()` — her çağrıyı bir önceki tam olarak yerleşene kadar bekleten basit bir `Future` kuyruğu — ekiplendi, tüm mutator'lar buna sarıldı. `test/features/persistence/player_progress_controller_test.dart`'taki "two concurrent refillBooster calls" testi düzeltmeyi geçici olarak geri alarak doğrulandı (testin gerçekten hatayı yakaladığı, yanlış-pozitif olmadığı teyit edildi).
- **`GoldKeyConstants.levelsPerGoldKeyReward` artık gerçekten bağlı (bu oturumda düzeltildi)**: Sabit tanımlıydı ama hiçbir yerde okunmuyordu — her 10 level tamamlamada bonus bir Altın Anahtar verilmesi gerektiğini ima ediyordu ama bu asla olmuyordu. `PlayerProgressController.advanceLevel`, tamamlanan level sayısı (`newLevel - 1`) bu sabite tam bölündüğünde artık `goldKeyCount`'u da +1 artırıyor — reklam izlemenin yanında ikinci, ilerlemeye dayalı bir Altın Anahtar kaynağı. GDD bu mekanizmayı metinde açıkça belirtmiyor; sabitin kendisi zaten kod tabanında duruyordu, bu yüzden onu gerçek bir davranışa bağlamak makul kabul edildi.
- **Klasik Mod rekoru artık anlık kaydediliyor (bu oturumda düzeltildi)**: Önceden yüksek skor yalnızca `RoundOutcomeClassicGameOver` (tahtada hiçbir parça sığmayınca) tetiklendiğinde kaydediliyordu — oyuncu geri tuşuna basıp round'u yarıda bırakırsa o skor hiç kalıcı olmuyordu. `GameController._apply` artık skor her arttığında (`state.score > previousScore`, Klasik Mod'da) `PlayerProgressController.recordClassicScore` çağırıyor; `_recordOutcome`'daki eski `RoundOutcomeClassicGameOver` dalı bu yüzden kaldırıldı (artık gereksiz — final skor zaten çoktan kaydedilmiş oluyor). Ayrıca `_RecordBadge` (game_screen.dart), gösterdiği "en iyi" değeri `math.max(persistedBest, session.score)` olarak hesaplıyor — persist'in async gecikmesini beklemeden, rekor kırıldığı anda rozet güncelleniyor.
- **Görsel cila geçişi (bu oturumda yapıldı)**: Kullanıcı geri bildirimi üzerine ("arka plan beyaz krem veya gri olmayacak... çok daha profesyonel gerçekçi tasarım istiyorum") hem ana menü hem oyun ekranı `WoodBackground` (procedural wood-grain) ile sarmalandı; board artık `GamePalette.bezelLight/bezelDark` gradyanlı açık-ahşap bir bezel içinde. Eklenenler: Klasik Mod'da taç+skor rekor rozeti (`_RecordBadge`, `GamePalette.recordGold`), her iki ekranda dişli/gear butonu → `SettingsSheet` (Ses/Titreşim toggle, `PlayerProgress.soundEnabled/hapticsEnabled` alanlarına kalıcı, `AudioService`/`HapticsService.setMuted` ile senkron), Level Mod ilerleme çubuğu artık Material `LinearProgressIndicator` değil ince çizgi + 900 eşik tick'i (`_LevelProgress`, test'te `Key('level-progress-bar')` ile bulunuyor), `BoosterBar` artık navy değil GDD mockup'ındaki gibi tan-ahşap panel + köşede şarj rozeti. Ana menüdeki Çerçeve/Altın Anahtar seçim sheet'leri artık `AppColors.paper` değil `AppColors.navy` arka plan kullanıyor (koyu tema ile tutarlı). Emülatörde ekran görüntüsüyle doğrulandı.
- **Sıra/çerçeve temizleme artık "kırılıp aşağı düşme" animasyonu (bu oturumda değiştirildi — kullanıcı talebi)**: Önceden temizlenen hücreler yerinde büyüyüp soluyordu ("outward burst"). `BoardGrid._clearingOverlay`/`_frameClearingOverlay` artık `Transform.translate` ile aşağı doğru **ivmelenen** (`t*t`, yerçekimi hissi) bir düşüş + `Transform.rotate` ile hafif savrulma + `Transform.scale` ile küçülerek kaybolma kullanıyor; yatay sürüklenme/dönüş miktarı `Random` değil hücrenin satır/sütununa göre deterministik bir hash'ten geliyor (aynı hücre her seferinde aynı şekilde düşer, ekstra state gerekmiyor). Hücrenin kendi `Stack`'i artık `clipBehavior: Clip.none` — aksi halde düşüş neredeyse hiç görünmeden 1-hücrelik sınırda kırpılırdı. **Bilinen sınırlama**: hücreler gerçek bir üst-katman overlay'i yerine kendi (satır/sütun sıralı boyanan) `Column`/`Row` hücresinde kaldığı için, çok satırlı bir temizlemede düşen bir karo teorik olarak ALTINDAKİ satırın (sonradan boyanan) opak karoları tarafından kısmen örtülebilir — düşüş mesafesi (`cellSize × 2.2`/`3.2`) ve solma eğrisi bunu en aza indirecek şekilde ayarlandı (karo, komşu satıra anlamlı şekilde taşmadan önce zaten büyük ölçüde solmuş oluyor), ama tam bir overlay-katmanı mimarisi kadar sağlam değil. Süreler de uzatıldı: `_clearBreakDuration` 320ms→480ms, `_frameBreakDuration` 520ms→650ms.
- **Gerçek toz patlaması eklendi (bu oturumda yapıldı)**: `BoardGrid`, `didUpdateWidget`'ta sıra temizleme/çerçeve yıkımı tespit ettiği her hücre için `Newton` widget'ına (`GlobalKey<NewtonState>`, grid `Container`'ını sarmalıyor) bir `woodDustBurst(...)` efekti ekliyor (`wood_dust_effect.dart` — `PhysicsEffectConfiguration`, `SquareShape`, ahşap paleti renkleri; `ExplosionPreset`'in yapısını taklit ediyor ama daire yerine kare parçacık ve hücre-boyutlu tohum sayıları kullanıyor). Çerçeve parçacıkları, görsel dalga ile aynı köşe-mesafesi gecikmesiyle (`Future.delayed`) ateşleniyor. Karar #4'teki scale/opacity overlay'ler DEĞİŞMEDİ — toz bunların üzerine ek bir katman. Emülatörde crash/exception olmadan doğrulandı (görsel patlamanın kendisi ekran görüntüsü zamanlamasıyla yakalanamadı, ama tetikleme mekanizması ve booster şarj tüketimi doğrulandı). **Test notu**: `Newton`'ın dahili `Ticker`'ı takılıyken (hiç efekt olmasa bile) sürekli çalışıyor, bu yüzden `board_grid_test.dart`'taki tüm `pumpAndSettle()` çağrıları asla sonlanmıyordu (timeout) — `await tester.pump(const Duration(seconds: 1))` ile değiştirildi (en uzun animasyonumuz 520ms, bir saniyelik pump tek seferde tamamlıyor).
- **Oynanış SFX'leri artık gerçek asset yerine prosedürel sentez kullanıyor**: `procedural_sfx.dart`, `game_audio.dart`'ın eşlediği 10 efektin (invalidMove, pieceSnap, lineComplete, multipleLineComplete, frameDestroy, pieceRotate, boosterActivate, woodCrack, levelComplete, gameOver) her biri için saf matematikle (sinüs/kare dalga + beyaz gürültü katmanları, hızlı-atak/üstel-sönüm zarfı) çalışma zamanında 16-bit mono PCM WAV byte'ları üretiyor. Asset lisansı riski yok (bkz. bir alttaki madde), `WoodBackground`'ın prosedürel doku yaklaşımıyla aynı felsefe. Kapsam dışında kalan efektler (ör. `woodMerge`, `victory`) hâlâ eski asset-yolu fallback'ine düşüyor. Ambient döngü (`playAmbient`) hâlâ asset bekliyor, prosedürel değil.
  - **Çalma yolu `BytesSource`'tan `DeviceFileSource`'a geçirildi (bu oturumda — kullanıcı "sesler hâlâ yok" dedi)**: `AudioPlayersAudioService` önceden WAV byte'larını doğrudan `BytesSource` ile çalıyordu; `BytesSource` audioplayers'ın Android tarafında `LOW_LATENCY`/`SoundPool` modunda açıkça desteklenmiyor (bkz. paket kaynağı, `error("Bytes sources are not supported on LOW_LATENCY mode yet.")`) ve genel olarak asset/URL kaynaklarına göre çok daha az kullanılan bir yol. `proceduralSfxPathFor` artık byte'ları `Directory.systemTemp` altında bir dosyaya yazıp (sonuç `SoundEffect` başına path olarak cache'leniyor) `DeviceFileSource` ile çalıyor — her asset/URL kaynağının zaten kullandığı, çok daha sağlam yol. Ayrıca ses karıştırma artık tepeyi yalnızca 1.0'ı AŞARSA aşağı kırpmıyor, her zaman tutarlı bir hedef tepeye (0.92) YUKARI da normalize ediyor — tek katmanlı sessiz efektler (ör. pieceRotate) artık çok katmanlılarla aynı algılanan yükseklikte.
  - **Doğrulama notu**: Gerçek cihazda/emülatörde sesin duyulup duyulmadığını AI olarak "duyamıyorum" — bunun yerine `adb logcat`'te dolaylı kanıt arandı: `I/AudioManager: dispatching onAudioFocusChange(-1)` satırları `xyz.luan.audioplayers.player.ModernFocusManager`'dan geliyor (yani gerçek bir `MediaPlayer` odak isteyip temiz şekilde bırakıyor), `android.hardware.audio@7.1-impl.ranchu` HAL katmanında gerçek frame tüketimi görülüyor, `flutter run` konsolunda ne kendi hata logumuz ne de audioplayers'ın kendi dahili logger'ı (`AudioLogger`, varsayılan olarak hata seviyesinde konsola basar) hiçbir şey basmıyor. Bu, sesin teknik olarak doğru çaldığına dair makul kanıt — kullanıcı hâlâ duymuyorsa muhtemel neden emülatör/cihaz ses yönlendirmesi (host ses seviyesi, emülatör sessize alma) veya efektlerin kısa/hafif algılanması, kod tarafında değil.
  - `test/core/services/audio/procedural_sfx_test.dart` WAV header doğruluğunu, tüm bağlı efektlerin bir tarifi olduğunu, cache'lemeyi VE artık dosyanın gerçekten diske yazıldığını doğruluyor.
- Asset lisansları: **Mixkit** SFX lisansının oyun içi kullanımı yasaklıyor olabileceği, **Zapsplat**'ın ücretsiz planda atıf zorunluluğu getirdiği tespit edildi — kullanılmadan önce manuel doğrulanmalı. (Prosedürel SFX'lerin eklenmesiyle bu risk yalnızca ambient döngü ve kapsam dışı efektler için geçerli kaldı.)
- Görsel/font varlıkları henüz yok (ses artık kısmen prosedürel — yukarı bak). Kalan asset-bağımlı çağrılar (`AssetSource`) `try/on Object catch` ile sarılı, eksik asset'te sessizce loglayıp devam ediyor (`appLogger.w`).
- Org kimliği `com.bbblock` yer tutucudur — mağaza gönderiminden önce gerçek ters-domain ile değiştirilmeli (`app/android`, `app/ios` proje ayarları).
- Android SDK cmdline-tools eksik, lisanslar kabul edilmedi (`flutter doctor` bunu gösteriyor) — cihazda/emülatörde çalıştırmadan önce tamamlanmalı.
- Git kimliği bu depoya özel ayarlandı (`digitalsynclab-cpu` / `digitalsynclab@gmail.com`); henüz uzak depo (GitHub vb.) yok.
- **Haptik + ses + animasyon bağlandı**: `GameController._apply`, her `GameEvent`'i hem `hapticIntensityFor` (`game_haptics.dart`) hem `soundEffectFor` (`game_audio.dart`) ile eşleyip sırasıyla `HapticsService` (`hapticsServiceProvider`, impl: `VibrationHapticsService`) ve `AudioService`'e (`audioServiceProvider`, impl: `AudioPlayersAudioService`) iletiyor. Testlerde platform kanalına/asset'e dokunmamak için `FakeHapticsService`/`FakeAudioService` kullanılıyor; emülatörde denendi (booster kullanımı üzerinden — hata logu yok). `BoardGrid` üç görsel geçişi doğrudan board diff'inden (`didUpdateWidget`) türetiyor: parça yerleştirme pop-in, sıra temizleme kırılma (+ gerçek toz patlaması), çerçeve yıkımı (daha büyük/yavaş/gecikmeli dalga + toz — karar #4). `GameScreen` ayrıca `ref.listen` ile skor artışını yakalayıp bir "+N" popup'ı yükseltip söndürüyor (`_ScorePopup`).
- **Sürükle-bırak**: Parça artık parmağın **üzerinde** (yaklaşık `dragCellSize × 1.3` yukarıda) beliriyor, tam altında değil — önceki `pointerDragAnchorStrategy` parçanın (0,0) hücresini işaretçiye çapalıyordu ve parmak, oyuncunun hedeflediği hücreleri kapatıyordu (bu oturumda düzeltildi, `piece_tray.dart`, özel bir `dragAnchorStrategy` ile). `BoardGrid._anchorFrom`, yerleştirme hücresini feedback widget'ının **raporladığı** konumdan hesapladığı için (`board_grid.dart`'a hiç dokunulmadı) bu değişiklik tamamen görsel/dokunsal — ne gösteriliyorsa oraya yerleşiyor, sadece artık parmağın üstünde gösteriliyor. Şekil tahtada kalacak şekilde clamp'leniyor. Geçersiz bir yere bırakma da `onPlace`'e ulaşıyor — engine tek doğruluk kaynağı, widget'taki `_validator` yalnızca sürükleme sırasındaki hayalet önizleme rengi için.
- **Test notu — tam ağaçta sürükle-bırak gesture'ı kırılgan**: `board_grid_test.dart`, izole bir `BoardGrid` üzerinde `startGesture`/`moveTo`/`up` ile drag-drop'u güvenilir şekilde test ediyor. Aynı yaklaşım tam `GameScreen` (MaterialApp+Scaffold+Overlay) ağacında hem manuel gesture hem `tester.drag()` ile denendi, ikisi de tutarsız hit-test sonuçları verdi (`tester.drag()` "hit test miss" uyarısı verdi). Çözüm: `GameScreen` testlerinde yerleştirme gerektiğinde gerçek drag simüle etmek yerine `ProviderScope.containerOf(context).read(gameControllerProvider(config).notifier).placePiece(...)` ile controller doğrudan çağrılıyor — drag mekaniği zaten `board_grid_test.dart`'ta kapsanıyor, burada asıl test edilen UI'ın state değişimine tepkisi.
- **Yön Değiştirme artık Swap gibi anında tüm tepsiye uygulanıyor (bu oturumda değiştirildi — kullanıcı talebi)**: Önceden "silahlan → tepsiden bir parça seç → o parça döner" akışıydı; kullanıcı bunun yerine tek dokunuşta **tüm kullanılmamış parçaların** dönmesini istedi — tıpkı Parça Değiştirme'nin zaten çalıştığı gibi. `GameEngine.rotatePiece(int trayIndex)` → `rotateTray()` oldu (tray'deki her `!isUsed` parçayı `RotatePieceCommand` ile döndürüyor, tek şarj harcıyor); `GameEvent.pieceRotated(trayIndex)` → parametre almayan `GameEvent.trayRotated()` oldu. `PieceTray`'in tüm "silahlan/hedefe dokun" mekanizması (`rotateArmed`, `onPieceTap`, altın parıltı vurgusu) tamamen kaldırıldı — artık yalnızca sürüklenebilir parçalar var. Tek Nokta Silici hâlâ tahtadaki bir hücreyi hedeflediği için "silahlan → hedefe dokun" akışını koruyor (`BoardGrid.removalArmed`/`onCellTap`); `GameScreen`'in `_ArmedBooster` enum'u bu yüzden `bool _removalArmed`'a sadeleşti.
- **Ödüllü Reklam gerçek akışa bağlandı (test ID'leriyle)**: Ana menüdeki "Ödüllü Reklam" çipi artık `adsServiceProvider` (impl: `AdMobAdsService`) üzerinden gerçek `showRewardedAd` çağırıyor; reklam hazır değilse SnackBar gösteriyor, ödül kazanılırsa `PlayerProgressController.grantGoldKey` tetikleniyor. `AndroidManifest.xml`/`Info.plist`'e Google'ın herkese açık **test** AdMob App ID'leri eklendi (ad unit ID'leri zaten testti) — ikisi de mağaza gönderiminden önce gerçek ID'lerle değiştirilmeli. iOS `SKAdNetworkItems` listesi henüz eklenmedi (attribution'ı etkiler, temel reklam akışını değil). Gerçek cihaz/emülatörde hiç denenmedi — Android SDK cmdline-tools eksik olduğu için bu makineden derlenemiyor bile.
- **Duraklatma (Pause)**: `GameScreen`, `WidgetsBindingObserver` ile uygulama arka plana alındığında (`paused`/`hidden`) round'u otomatik duraklatıyor; ayrıca header'da elle duraklatma butonu var. Devam etmek için oyuncunun "Devam Et"e dokunması gerekiyor (kazara dokunmaları önlemek için). Kullanılmayan, hiçbir yerde referans edilmeyen eski `game_state/domain/GameState` (Freezed union) kaldırıldı — bu pause implementasyonu onun yerini gerçek bir ihtiyaçla dolduruyor. Not: `flutter test`'te `AppLifecycleState.paused` simüle edilirken engine kareleri bilinçli olarak bastırıyor (gerçek OS davranışını taklit ediyor) — testte `resumed`'a dönülmeden overlay görünmez, bu gerçek cihaz davranışıyla tutarlı.
- **Tahta 9x9'dan 10x10'a büyütüldü (bu oturumda — kullanıcı önceki 9x9 talimatının yanlış olduğunu belirtti)**: Tek değişiklik `BoardConstants.gridSize` (2. karardan beri zaten tek doğruluk kaynağıydı) — `Board.empty()`/`Board.framed()` bunu varsayılan alıyor, `BoardGrid`/`GameScreen` hücre boyutunu `boardSide / session.board.size` ile dinamik hesaplıyor, hiçbir yerde 9 hardcode edilmemişti. Tek gerçek kod etkisi: `game_engine_test.dart`'taki "completing a line clears it" testi 9 karakterlik satır string'leriyle (`boardFromRows`) kendi 9 genişliğinde tahtasını kuruyordu — bu artık 10'a güncellendi (satır/skor beklentileri dahil). GDD dosyasına dokunulmadı (bkz. dosyanın en üstü).
- **Başlangıç Altın Anahtar bakiyesi 0'dan 10'a çıkarıldı (bu oturumda — kullanıcı talebi)**: `GoldKeyConstants.startingGoldKeyCount = 10`, `PlayerProgress.goldKeyCount`'un `@Default`'u oldu (`dart run build_runner build` ile yeniden üretildi — `player_progress.g.dart`'taki `?? 0` düşmesi otomatik `?? 10`'a döndü, json_serializable `@Default` ifadesini olduğu gibi kopyaladığı için). Anahtarlar harcandıkça kalıcı olarak azalıyor; hesap genelinde tek yenileme yolu **temiz bir silme+yeniden yükleme** — `PlayerProgress` tek yerel `shared_preferences` JSON blob'u olduğu için (Supabase/bulut senkronu henüz yok) uygulamayı silmek gerçekten sıfırlıyor, bu davranış için ekstra kod gerekmedi, yalnızca varsayılan değer. Fresh-install senaryosu emülatörde `pm clear` ile doğrulandı (10 ile başlıyor, harcanınca kalıcı azalıyor). Etkilenen testler: `player_progress_controller_test.dart`'taki "fresh repository" senaryoları (artık `GoldKeyConstants.startingGoldKeyCount` bazlı bekliyor, `refillBooster` "zero keys" testi artık `goldKeyCount: 0`'ı açıkça geçiyor) ve `widget_test.dart`'taki reklam-ödülü testi.
- **Oyun içi HUD, yeni tek referans görsel + html/css koduna göre "birebir" yeniden tasarlandı (bu oturumda — kullanıcı talebi "birebir örnek al... arayüz olarak komple")**: Önceki tur "stil ilhamı al ama kendinden tasarım kat" idi (bkz. yukarıdaki dosya geçmişi notu); bu kez kullanıcı `css.txt`'teki hex değerlerini bizzat kaynak olarak kullanmayı istedi. `GamePalette`'e CSS'ten birebir örneklenen yeni token'lar eklendi (`boardBackground`/`emptySlot` artık `.grid-container`/`.cell`'in kendi tonları, `bezelLight/bezelDark` artık `.game-container`'ın gradyanı, `woodButtonLight/Dark/Border` `.icon-btn`'in gradyanı, `panelDark/panelDarkBorder` `.score`/`.powerup-btn`/`.piece-dock`'un ortak koyu-kahve dolgusu, `buttonLedge` düz — bulanık olmayan — `box-shadow: 0 Npx 0 #321808` "3B buton" gölgesi, `progressFillLight/Dark` + `progressTrack` `.progress-fill`/`.progress-bar`). `game_screen.dart`: `_RoundIconButton` navy daireden bu wood-gradyanlı köşeli-kare + ledge-gölge butona geçti; `_RecordBadge` navy yerine `panelDark` kullanıyor; `_LevelProgress` ince çizgiyi bırakıp `.progress-bar`'ın kalın (12px) yuvarlak gradyan barına döndü (900 eşik çizgisi referansta yok ama gerçek oyun bilgisi olduğu için korundu); yeni `_GoldKeyChip` Level Mod header'ına eklendi — referansın sandık/chest ikonunun karşılığı olarak gerçek Altın Anahtar sayısını gösteriyor (sandığın kendisinin oyunda bir karşılığı yok). `booster_bar.dart`: `_BoosterButton` önceki dairesel "madalyon" tasarımından (3153591 commit'i, bir önceki tur) `.powerup-btn`'in yatay pilline geçti; `_ChargeBadge` artık `.add-btn`'in köşeli-kare gradyan rozeti. Tek Nokta Silici ikonu `PhosphorIconsBold.eraser`'dan referanstaki 💣'ye karşılık gelen `PhosphorIconsBold.bomb`'a değişti. `piece_tray.dart` artık parçaları `.piece-dock`'un koyu-kahve panelinde (`GamePalette.panelDark`, kenarlık `panelDarkBorder`) gösteriyor — önceden hiçbir arka plan paneli yoktu, parçalar doğrudan masa dokusunun üzerinde duruyordu. `wood_tile.dart`'a referans karolarındaki gibi yumuşak sol-üst vurgu (`GamePalette.tileHighlight` gradyanı) eklendi, hem tahtadaki hem tepsideki karoları etkiliyor. Ortak `PressableScale` widget'ı (`pressable_scale.dart`) eklendi — dokunulunca hafifçe küçülüp bırakılınca geri sıçrıyor, referansın "3B buton" hissini header ve booster butonlarına taşıyor.
  - **İlk kapsam kararı (shuffle/minus butonları — hâlâ geçerli)**: Referansın shuffle (🔀) ve minus (➖) genel-amaçlı butonları eklenmedi — motor yalnızca 3 gerçek tamamlayıcı biliyor (Yön/Değiştir/Sil), karşılığı olmayan buton eklenmedi; mevcut 3 buton bunun yerine hepsi `.powerup-btn` pilinin tutarlı tek ailesiyle stillendi.
  - **Bulunan gerçek bug (bu oturumda, uygulama emülatörde koşarken yakalandı)**: `_LevelProgress`'in track'i ilk yazımda `DecoratedBox(child: SizedBox(height: 12))` idi — `Container`'ın aksine `DecoratedBox`'ın "child yoksa mevcut alanı doldur" davranışı yok, `SizedBox`'ın da genişliği belirtilmemişti, bu yüzden track genişliği 0'a çöküp görünmez oluyordu (yalnızca 900 eşik çizgisinin ince "|"i ve collapsed border'ın kendisi görünüyordu). `Container(height: 12, decoration: ...)` (child'sız, width'siz) ile değiştirildi — bu, kod tabanının orijinal ince-çizgi versiyonunun zaten kullandığı (ve çalışan) desendi. Emülatörde ekran görüntüsüyle doğrulandı (düzeltmeden önce ve sonra).
  - **İkinci tur — kullanıcı ilk sonucu beğenmedi ve aynı görsel + kodu tekrar gönderdi**: Kullanıcı "şuanda yaptığını beğenmedim" dedi ve Gemini'den aldığı html/css'i tekrar attı (içerik birebir aynı çıktı — kaynak fark etmiyordu, sorun uygulanışıydı). Emülatör ekran görüntüsünü referansla yan yana karşılaştırınca asıl fark ortaya çıktı: referans hepsi TEK bir `.game-container` panelinde (ortak gradyan, 4px kenarlık, 20px radius, sıkı 12px boşluklar) toplanmış bütün bir kart; bizim ekranımızda ise header/board/tray/booster ayrı ayrı `WoodBackground` dokusunun üzerinde yüzüyordu (aralarda dokulu arka plan görünüyordu) ve controls (booster) sırası tersti (referans grid → controls → piece-dock, biz grid → piece-dock → controls yapmıştık). Ayrıca footer ("Wood Block PUZZLE" + Shop) hâlâ hiç yoktu.
    - Düzeltme: `GameScreen` artık her şeyi (header, board, controls, dock, footer) TEK bir `Container`'da topluyor (`GamePalette.bezelLight/bezelDark` gradyan, `panelDark` 4px kenarlık, radius 20, dış SafeArea boşluğu 16'dan 10'a indi, iç boşluklar 12'ye sıkıldı). `BoardGrid` kendi ayrı bezel katmanını kaybetti — `.grid-container` artık gerçekten `.game-container`'ın üzerine oturuyor (yeni `GamePalette.gridBorder` `#241105` ile), önceki iki katmanlı (bezel+board) gradyan çakışması gitti. Controls/piece-dock sırası board→booster→tray olarak düzeltildi (referansla eşleşiyor). Yeni `_Footer` widget'ı eklendi: sol altta "BB Block"/"PUZZLE" iki satırlı başlık (referansın "Wood Block"/"PUZZLE" yapısını taklit ediyor), sağ altta referansın Shop/coin konumunda gerçek Altın Anahtar rozeti (`_GoldKeyFooterBadge`, `.shop-btn` gradyanı + içinde koyu pil) — sahte bir coin/shop eklenmedi, gerçek para birimimiz o konuma taşındı (önceki turda header'a eklenen `_GoldKeyChip` kaldırıldı, yerini bu aldı).
  - Emülatörde hem Level Mod (10x10 tahta + çerçeve + tek panel + doğru sıra + booster pilleri + footer'daki Altın Anahtar rozeti) hem Klasik Mod (Çerçeve YOK, taç rozetli skor, aynı tek panel) `pm clear` ile taze hesapta ekran görüntüsüyle doğrulandı; 93/93 test geçiyor, `flutter analyze` temiz.
- **Uygulama ikonu, kullanıcının sağladığı `images/BB_Block_İcon.png`'den üretildi (bu oturumda, sonra düzeltildi)**: `flutter_launcher_icons` paketi eklendi (`pubspec.yaml`'da `flutter_launcher_icons:` config bloğu, `image_path: assets/icon/icon.png`), `dart run flutter_launcher_icons` çalıştırılıp Android/iOS için tüm boyutlar otomatik üretildi. **İlk denemede bulunan gerçek sorun**: `adaptive_icon_background`/`adaptive_icon_foreground` bilinçli olarak atlanmıştı (kaynak görselin zaten yumuşak yuvarlatılmış köşeleri olduğu gerekçesiyle) — ama bu, modern Android launcher'ların (Pixel, One UI...) adaptive-olmayan bir ikonu KENDİ maskesine sarıp DÜZ BEYAZ bir arka plan koymasına yol açtı (kullanıcının gördüğü "etrafı beyaz kalmış" sorunu). Düzeltme: `adaptive_icon_background: "#2A160C"` (ikonun kendi koyu ahşap tonu) + `adaptive_icon_foreground: assets/icon/icon.png` eklendi, `flutter_launcher_icons` yeniden çalıştırıldı — artık `android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml` gerçek bir adaptive-icon XML'i (%16 inset) ve `values/colors.xml` kendi arka plan rengini tanımlıyor. Emülatörün launcher'ında (diğer uygulamaların hepsi beyaz daire kullanırken) artık kendi koyu temalı dairesel ikonuyla doğru göründüğü doğrulandı.
- **Ana menü, kullanıcının sağladığı `images/BB_Block_Anasayfa.png` üzerine kuruldu (bu oturumda, iki kez revize edildi)**: Görsel `assets/images/home_background.png`'a kopyalandı. **İlk tur**: görselin üstündeki, oyunun gerçek adıyla uyuşmayan baked-in "BEN BRICK BLOCKS" tahta tabelasını gizlemek için `Transform.scale(scale: 1.75)` ile agresif bir kırpma uygulandı. **Kullanıcı bunu beğenmedi** ("çok zoom şeklinde olmuş... tam ekrana sığdır", orijinal görseli referans olarak ekleyip karşılaştırdı) — 1.5'e düşürüldüğünde bu sefer tabelanın bir dilimi geri geldi, ki bu matematiksel olarak kaçınılmaz: görselin en-boy oranı (853×1844) telefon ekranına o kadar yakın ki (BoxFit.cover doğal olarak neredeyse hiçbir şeyi kırpmıyor), tabelayı TAM gizlemek İLE sahneyi TAM göstermek (yakınlaştırmadan) aynı anda sağlanamıyor — biri diğerini feda ediyor. **Nihai karar**: kullanıcının açık talimatı ("görsel böyle tam ekran olmalı") önceliklendirildi — `Transform.scale` tamamen kaldırıldı, görsel artık düz `BoxFit.cover` ile olduğu gibi (tabela dahil) gösteriliyor, yalnızca durum çubuğu okunabilirliği için çok hafif bir üst gradyan (`Colors.black45`, 60px) kaldı. **Bilinçli ödün**: yanlış oyun adı ("BEN BRICK BLOCKS") artık ekranda görünüyor — kullanıcı zoom'u iki kez açıkça reddetti ama metin uyuşmazlığını hiç gündeme getirmedi, bu yüzden en son ve en açık talimat önceliklendirildi. Kullanıcı görseli değiştirir/düzeltirse bu otomatik çözülür.
  - **Bununla bağlantılı gerçek bug**: Görselin kendi büyük başlık grafiği ile ekranın kendi "BB Block" metin başlığı aynı bölgede çakışıyordu (iki farklı oyun adı üst üste). Çözüm: `HomeScreen`'deki ayrı "BB Block" `Text` widget'ı tamamen kaldırıldı (görsel zaten kendi başlığını taşıyor) — bkz. `test/widget_test.dart`'ta buna göre güncellenen test.
  - **`PremiumGameButton`** (`features/home/presentation/widgets/premium_game_button.dart`): kullanıcının verdiği CSS spesifikasyonunu (72px yükseklik, 22px radius, 8px koyu ceviz ahşap çerçeve, 3 tonlu camsı iç gradyan, üstte %32 beyaz parlaklık, altta koyu iç gölge taklidi, basınca 0.96 ölçek + 4px aşağı kayma + spring bounce) birebir uyguluyor. Level Mod = yeşil (`#8DE25C→#5DBE38→#3C9626`, kullanıcının verdiği tam değerler), Klasik Mod = mavi (`#6FD1F5→#2E9FE0→#1B6FA8`, aynı parlaklık/canlılık profiliyle yeşile paralel seçildi). Flutter'da native inset-shadow olmadığı için üst parlaklık/alt gölge, kenarlara yaslanmış yarı saydam gradyan `DecoratedBox`'larla taklit ediliyor.
  - Ana menünün Çerçeve seçim etiketleri güncellendi: **"Çerçeve Var (8x8)" / "Çerçeve Yok (10x10)"** — aşağıdaki Klasik Mod tahta boyutu maddesine bakın.
- **Klasik Mod artık çerçeve seçimine göre farklı tahta boyutu kullanıyor (bu oturumda — açık kullanıcı talimatı)**: Çerçeve VAR artık **8x8**, Çerçeve YOK **10x10** (Level Mod'un boyutuyla aynı) — önceden ikisi de tek bir global `BoardConstants.gridSize`'a (10) bağlıydı. Yeni `BoardConstants.classicFramedGridSize = 8` eklendi; `ClassicModeStrategy.createInitialBoard()` artık `hasFrame ? Board.framed(size: BoardConstants.classicFramedGridSize) : Board.empty()` (frameless hâlâ `gridSize` varsayılanını kullanıyor). `Board.empty()`/`Board.framed()` zaten `size` parametresi alıyordu (2. karardan beri), bu yüzden değişiklik tek satırlık kaldı. `BoardGrid`/`GameScreen` hücre boyutunu `boardSide / session.board.size` ile zaten dinamik hesapladığı için düzen tarafında hiçbir değişiklik gerekmedi — 8x8 ve 10x10 aynı widget ağacında otomatik doğru render ediliyor. Puanlama sabitleri (`ScoringConstants.classicFramedLineScore = 8` vb.) DOKUNULMADI — bunlar sıra başına düz bir ödül, hücre sayısına bağlı değil. Emülatörde her iki boyut da (`pm clear` sonrası taze hesapla) ekran görüntüsüyle doğrulandı. Çerçeveli/çerçevesiz en yüksek skorlar zaten ayrı ayrı tutuluyordu (`PlayerProgress.classicHighScoreFramed/Frameless`, taç rozetiyle gösteriliyor) — bu kısım için yeni kod gerekmedi, kullanıcının "ikisininde en çok ulaşılan puanı farklı olacak" isteği zaten karşılanıyordu.
- **Game Feel Engine (bu oturumda kuruldu — kullanıcının detaylı "Game Feel" spesifikasyonuna karşılık, `core/game_feel/`)**: Kullanıcı sesin/animasyonun/parçacığın/haptiğin/kamera sarsıntısının "tek bir sistem" olarak tasarlanmasını, `gameFeel.play(GameEvent.blockPlaced)` şeklinde tek bir giriş noktası olan bir "Game Feel Engine" modülü önerdi. Kurulan mimari, önerilen ismi ve API şeklini birebir uyguluyor:
  - **`FeedbackOrchestrator`** (`core/game_feel/feedback_orchestrator.dart`): `play(GameEvent event)` — tek bir `GameEvent` için haptik + birincil ses + (varsa) katmanlı ikincil ses + (varsa) ekran sarsıntısını AYNI yerden tetikliyor. `GameController._apply`, önceden ayrı ayrı çağırdığı `_triggerHaptics`/`_triggerAudio`'yu kaldırıp `events.forEach(ref.read(feedbackOrchestratorProvider).play)`'e indirgendi — motor katmanı artık "ne olduğunu" söylüyor, "nasıl hissettirdiğini" bilmiyor.
  - **`game_feel_mapping.dart`** (`features/game/application/`): `hapticIntensityFor`/`soundEffectFor`'un (değişmeden duran) yanına iki yeni saf eşleme fonksiyonu eklendi — `secondarySoundEffectFor` (katmanlı ikinci ses; ör. `piecePlaced` artık `pieceSnap` ÜSTÜNE `pieceDrop` da çalıyor, çok satırlı temizlemeler `woodExplosion` katmanı alıyor) ve `screenShakeMagnitudeFor` (etkiye göre ölçeklenen sarsıntı büyüklüğü — tek parça yerleştirme 1.5, çok satırlı temizleme satır sayısıyla artıyor, çerçeve yıkımı 9, level tamamlama 6).
  - **`ScreenShakeController`/`ScreenShake`** (`core/game_feel/screen_shake.dart`): `ChangeNotifier` tabanlı basit bir event bus (`shake(magnitude)` → generation sayacı artırır) + bunu dinleyip sönümlenen bir sinüs sarsıntısına çeviren bir widget (`Transform.translate`, kare-dalga değil gerçek decaying-sine). `GameScreen`'in tüm oyun kartı (header+tahta+kontroller+tepsi+footer) bu widget'a sarıldı — bir çarpışma olduğunda tüm panel hafifçe titriyor.
  - **`SpringPressable`** (`core/game_feel/spring_pressable.dart`, önceki `pressable_scale.dart`'ın yerini aldı — silindi): Artık `ConsumerStatefulWidget`. Basışta gerçek bir `SpringSimulation` ile (kanvas eğrisi değil) bırakışta hafif "boing" sağlıyor (deliberate unclamped overshoot). **Kritik**: basış anında otomatik olarak `SoundEffect.buttonClick` + hafif haptik çalıyor — bu sayede uygulamadaki HER buton (header ikonları, booster butonları, `PremiumGameButton`, start sheet butonları) tek bir yerden, çağıran kodun hatırlamasına gerek kalmadan "her butonun basış sesi olmalı" isteğini karşılıyor.
  - **Prosedürel ses paleti tamamlandı**: `SoundEffect` enum'undaki (önceden 29 değerin yalnızca 10'u tarifli) TÜM 19 eksik tarif eklendi (`buttonClick`, `buttonHover`, `piecePickUp`, `pieceDrag`, `pieceDrop`, `combo`, `scoreIncrease`, `victory`, `reward`, `goldenKey`, `boosterFinish`, `popupOpen`, `popupClose`, `screenTransition`, `woodHit`, `woodMerge`, `woodSlide`, `woodBreak`, `woodExplosion`) — artık her `SoundEffect` değerinin gerçek bir prosedürel tarifi var, hiçbiri eksik-asset fallback'ine düşmüyor. `procedural_sfx_test.dart` artık TÜM `SoundEffect.values`'ı tek tek doğruluyor (önceki "wired subset" listesi kaldırıldı — artık hepsi "wired").
  - **Parça tutma/sürükleme geri bildirimi**: `PieceTray` artık `ConsumerWidget` — `Draggable.onDragStarted`'da `piecePickUp` + `pieceDrag` sesleri ve hafif haptik çalıyor; sürüklenen parça artık düz bir kopya değil, `_GrabbedPiece` (%105 ölçek + altın parıltı glow + alt gölge) — GDD'nin "Block Grab" maddesine karşılık geliyor.
  - **Level Complete flourish**: `_RoundOverlay` artık `ConsumerStatefulWidget` — zafer anında (`RoundOutcomeLevelComplete`) 3 kademeli konfeti patlaması (`confettiBurst`, `wood_dust_effect.dart`'a eklendi — altın/krem tonlarında, `woodDustBurst`'ten çok daha büyük/uzun ömürlü), kısa bir altın ekran flaşı (`_VictoryFlash`, gerçek `AnimationController` + `TweenSequence`), altın kenarlıklı kart, ve skor artık `TweenAnimationBuilder<int>` ile sayarak yükseliyor (statik metin yerine). Oyun bitti/level başarısız durumları kasıtlı olarak daha sade kaldı (yalnızca zafer bu ekstra parlaklığı hak ediyor).
  - **Kapsam dışı bırakılanlar (dürüstçe not edilsin)**: Kullanıcının 17 maddelik spesifikasyonundaki bazı çok spesifik detaylar bu turda uygulanmadı — gerçek "80ms hit-freeze" (kare dondurma) yok, Rotate booster için gerçek "motion blur"/"rotation trail" shader'ı yok (bunun yerine dönme zaten anlık; ayrı bir görsel iz eklenmedi), geçersiz bir bırakışta parçanın fiziksel olarak "geri sekmesi" eklenmedi (`DragTarget.onWillAcceptWithDetails` her zaman kabul ediyor — bkz. "Sürükle-bırak" maddesi; bunu değiştirmek geçersiz-hamle ses/haptik'inin motor üzerinden değil widget'tan tetiklenmesini gerektirirdi, mimariyi bozardı), ve "Combo" için ayrı bir seri/streak sayacı yok (motor katmanı zaten "combo" kavramını tutmuyor — karar #7 puanlamayı etkilemeyeceğini zaten söylüyor, yeni bir streak-tracking state eklemek bu turun kapsamının ötesindeydi). Asset araştırması (madde 17) ayrı bir web taraması olarak yapılmadı — mevcut prosedürel-sentez yaklaşımı (lisans riski taşımayan, zaten kanıtlanmış) genişletilerek aynı ihtiyacı karşıladı.
  - Emülatörde tüm akış (Level Mod anahtarla giriş → booster kullan → logcat) crash/exception olmadan doğrulandı; 90/90 test geçiyor, `flutter analyze` temiz.
- **`GlassPanel` — frosted-glass panel birincil hale geldi (bu oturumda, kullanıcı geri bildirimi üzerine)**: Kullanıcı ana sayfa/ayarlar/duraklatma gibi alanları "çok basit" buldu (yalnızca `PremiumGameButton`'ları hariç tuttu — "girişteki butonlarımız mükemmel"). Yeni `core/theme/glass_panel.dart`: `BackdropFilter` blur (sigma 18) + yarı saydam `AppColors.navy` dolgu + ince beyaz kenarlık + yumuşak gölge — tek bir yerden tüm uygulamaya tutarlı bir "cam panel" dili katıyor. Kullanıldığı yerler: `HomeScreen._BestScores` (artık düz metin yığını değil, ikon+değer+etiket'li 3 hücreli bir satır — `_StatItem`/`_StatDivider`; konumu da düzeltildi: eskiden karakterin yüzünün üzerinde ortada yüzüyordu, artık butonların hemen üstünde), `SettingsSheet` (artık düz `AppColors.navy` modal değil, cam panel + kart-stili toggle satırları), `_FrameChoiceSheet`/`_LevelStartSheet` (aynı cam panel), `GameScreen`'in `_PauseOverlay`/`_RoundOverlay`'i (eski düz `Card(color: AppColors.navy)` yerine). Yeni paylaşılan `_OverlayPrimaryButton` (altın gradyan + `SpringPressable`) `FilledButton`'ın yerini pause/round-over'da aldı. `_SheetChoiceButton` (Çerçeve Var/Yok) da aynı cam-uyumlu dokunulabilir stile geçti.
  - **Kaldırılan**: `HomeScreen`'deki ayrı "BB Block" başlık metni (yukarıdaki ana sayfa görseli maddesine bakın — görselin kendi başlığıyla çakışıyordu).
  - `test/widget_test.dart`'taki "home screen shows the game title..." testi güncellendi (artık yalnızca mod butonlarını kontrol ediyor).

## Komutlar

```bash
cd app
flutter pub get
dart run build_runner build   # .freezed.dart / .g.dart üretimi (--delete-conflicting-outputs artık gerekmiyor)
flutter analyze
flutter test
```

## Referans raporlar

Bu depo dışında, konuşma geçmişinde iki analiz artifact'i üretildi: proje envanteri + GDD tutarsızlıkları, ve mimari/teknoloji karar raporu (açık kaynak + asset araştırması dahil). Yeni bir oturumda bu dosyaları bulamazsan, kullanıcıdan bağlantıları isteyebilirsin.
